/* Known to fail with nodent-transform@3.2.1. See https://github.com/MatAtBread/nodent/issues/108 
async function nop(x) { return x }

const list = [ 'a','b','c' ];

var count = list.length ;
var resolve ;
var p = new Promise(function(r){ resolve = r }) ;
var s = "" ;
for(let v of list) {
	await nop() ;
	setTimeout(function(){
		s += v ;
		count-- ;
		if (count<=0)
			resolve(s) ;
	}, 0);
}
return p ;
*/
return true ;